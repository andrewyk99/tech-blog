<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Tech Blog</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheets/style.css">
        <link rel="stylesheet" href="/stylesheets/login.css">
    </head>

    <body>
        <div class="wrapper">
            <header>
                <nav class="flex">
                    <a href="/" class="btn" role="button">Home</a>
                    {{#if loggedIn}}
                    <a href="/dashboard" class="btn" role="button">Dashboard</a>
                    <button id="logout" role="button" class="btn">Logout</button>
                    {{else}}
                    <a href="/login" role="button" class="btn">Login</a>
                    {{/if}}
                </nav>
                
                <div class="row col-3 d-inline-flex container rounded title">
                    {{#if homepage}}
                    <h1 class="rounded">The Tech Blog</h1>
                    {{else}}
                    <h1 class="rounded">Your Dashboard</h1>
                    {{/if}}
                    
                </div>
            </header>

            <main>
                {{{ body }}}
            </main>
            <footer class="footer">
                © 2022 by Andrew Kim
            </footer>
        </div>

        {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
        {{/if}}
    </body>
</html>